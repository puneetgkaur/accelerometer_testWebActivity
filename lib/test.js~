/*var fs = require('fs'),
    readline = require('readline'),
    stream = require('stream');
var path = require('path');
var filePath1 = path.join(__dirname + '/sugar-web/env.js');
var filePath2 = path.join(__dirname + '/sugar-web/env1.js');
var instream = fs.createReadStream(filePath1);
var outstream = fs.createWriteStream(filePath2);
//var str_content_start="/*CONTENT START"
//var str_content_end="/*CONTENT END"


outstream.readable = true;
outstream.writable = true;

var rl = readline.createInterface({
    input: instream,
    output: outstream,
    terminal: false
});

rl.on('line', function(line) {
    console.log(line);
    //Do your stuff ...
    //Then write to outstream
    rl.write(line);

});
*/
var fs = require('fs');
var path = require('path');
var filePath1 = path.join(__dirname + '/sugar-web/env.js');
var filePath2 = path.join(__dirname + '/sugar-web/env1.js');

fs.readFileSync(filePath1).toString().split('\n').forEach(function (line) { 
    console.log(line);
    fs.appendFileSync(filePath2, line.toString() + "\n");
});
